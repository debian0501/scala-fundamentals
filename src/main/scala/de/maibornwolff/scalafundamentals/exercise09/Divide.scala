package de.maibornwolff.scalafundamentals.exercise09

object Divide {

  /*
  * Implement this method. It accepts two Int parameters and divides one by the other.
  * Use Option to avoid exceptions when the denominator is 0
  */
  def divide(numerator: Int, denominator: Int) : Option[Int] = ???

  /*
   * Using your divide method and a for comprehension.
   * Write a method called divideOptions that accepts two parameters of type Option[Int] and divides one by the other:
   */
  def divideOptions(numerator: Option[Int], denominator: Option[Int]): Option[Int] = ???
}
